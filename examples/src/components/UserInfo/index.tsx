import * as React from "react";
import { Button } from "antd";

const User: React.FC = () => {
  return (
    <div>
      User
      <Button type="primary">测试按钮</Button>
    </div>
  );
};

export default User;
